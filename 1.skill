FlutterNativeBase 跨平台原生桥接基础工程技能文档
版本：1.0.0
适用范围：Flutter 3.38+，支持 Android/iOS/Windows/macOS/Linux
核心理念：零配置添加 FFI 接口，一次定义，全平台编译，内置国际化与品牌标识

1. 项目信息
项目名	flutter_native_base
包名（Android 应用 ID）	com.naipingzai.flutter_native_base
仓库	https://github.com/naipingzai/FlutterNativeBase
说明	提供 Flutter 与 C/C++ 原生代码的高效桥接，支持多语言（简体中文/英文），统一品牌图标。
2. 固定目录结构（不可变动）
text
flutter_native_base/
├── lib/
│   ├── flutter_native_base.dart        # 库入口，导出所有公开API
│   ├── l10n/                           # 国际化资源
│   │   ├── app_en.arb                  # 英文字符串
│   │   ├── app_zh.arb                  # 简体中文字符串
│   │   └── l10n.dart                   # 自动生成（忽略）
│   └── src/
│       ├── ffi/
│       │   ├── native_bindings.dart    # FFI 函数声明与加载
│       │   ├── native_types.dart       # 类型映射与错误码
│       │   └── native_service.dart     # 封装后的 Dart 服务类
│       └── ui/
│           └── localizations.dart      # 扩展本地化委托（可选）
│
├── native/                             # 所有原生代码（C/C++）
│   ├── include/                        # 公开头文件（接口定义）
│   │   └── bridge_api.h                # 唯一接口头文件
│   ├── src/                            # 实现代码
│   │   ├── core/                       # 平台无关的业务逻辑
│   │   │   ├── math_utils.c
│   │   │   └── string_utils.c
│   │   ├── platform/                   # 平台适配层（条件编译）
│   │   │   ├── file_io_android.c
│   │   │   ├── file_io_apple.m
│   │   │   └── file_io_windows.c
│   │   └── bridge_api.c                # 接口实现
│   └── CMakeLists.txt                  # 仅桌面端，模板自动生成，禁止修改
│
├── assets/                             # 应用资源
│   └── icons/
│       └── flutter_native_base_logo.jpg # 应用图标（源文件）
│
├── android/                            # Android 特定（包名已配置）
├── ios/                                # iOS 特定（Bundle ID 已配置）
├── windows/                            # Windows 特定
├── macos/                              # macOS 特定
├── linux/                              # Linux 特定
├── pubspec.yaml                        # 依赖与资产配置
├── l10n.yaml                           # 国际化生成配置
└── ffigen.yaml                         # 可选：自动生成绑定代码
重要：所有对外 FFI 接口必须且只能定义在 native/include/bridge_api.h 中。新增接口时只需修改此头文件和对应的 .c 实现，无需改动任何构建脚本。

3. 国际化配置（必做）
3.1 添加依赖
在 pubspec.yaml 中添加：

yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
3.2 创建 ARB 文件
在 lib/l10n/ 目录下创建两个文件：

app_en.arb

json
{
  "@@locale": "en",
  "appTitle": "Flutter Native Base",
  "helloWorld": "Hello, World!",
  "counterLabel": "Button tapped {count} times",
  "@counterLabel": {
    "placeholders": {
      "count": {
        "type": "int",
        "format": "decimal"
      }
    }
  },
  "nativeAddResult": "Native add result: {result}"
}
app_zh.arb

json
{
  "@@locale": "zh",
  "appTitle": "Flutter 原生基础",
  "helloWorld": "你好，世界！",
  "counterLabel": "按钮点击了 {count} 次",
  "nativeAddResult": "原生加法结果：{result}"
}
3.3 配置 l10n.yaml
在项目根目录创建 l10n.yaml：

yaml
arb-dir: lib/l10n
template-arb-file: app_en.arb
output-localization-file: l10n.dart
synthetic-package: false
3.4 生成本地化代码
运行命令：

bash
flutter gen-l10n
此命令会生成 lib/l10n/l10n.dart，包含 AppLocalizations 类。

3.5 在 MaterialApp 中配置
在 lib/main.dart（或应用的入口文件）中配置：

dart
import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'l10n/l10n.dart';

void main() => runApp(const MyApp());

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Native Base',
      localizationsDelegates: const [
        AppLocalizations.delegate,
        GlobalMaterialLocalizations.delegate,
        GlobalWidgetsLocalizations.delegate,
        GlobalCupertinoLocalizations.delegate,
      ],
      supportedLocales: const [
        Locale('en'),
        Locale('zh'),
      ],
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: const MyHomePage(),
    );
  }
}
3.6 使用本地化字符串
在 Widget 中通过 AppLocalizations.of(context) 获取：

dart
Text(AppLocalizations.of(context)!.helloWorld),
必须遵守规则：

所有显示文本（包括标题、按钮、提示、错误信息）都必须从 ARB 文件中读取，严禁硬编码。

数值占位符（如 {count}）必须正确使用，确保在英文和中文下都能正确格式化。

确保应用启动时，根据系统语言自动切换，或提供手动切换设置（可在后续扩展）。

4. 应用图标配置
4.1 准备图标源文件
将 flutter_native_base_logo.jpg 放置在 assets/icons/ 目录下。建议使用 flutter_launcher_icons 插件自动生成各平台图标。

4.2 添加依赖与配置
在 pubspec.yaml 中添加 dev_dependencies：

yaml
dev_dependencies:
  flutter_launcher_icons: ^0.13.1
在 pubspec.yaml 末尾添加配置：

yaml
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/flutter_native_base_logo.jpg"
  min_sdk_android: 21
  remove_alpha_ios: true
4.3 生成图标
运行命令：

bash
flutter pub get
flutter pub run flutter_launcher_icons
此命令会自动替换 Android mipmap 文件夹和 iOS Assets.xcassets 中的 AppIcon，无需手动修改。

4.4 验证
检查 android/app/src/main/res/mipmap-* 和 ios/Runner/Assets.xcassets/AppIcon.appiconset 是否已更新。

5. 接口定义规范（C 侧）
（此部分与之前版本一致，保持不变。仍使用 bridge_api.h 和 bridge_api.c 作为唯一接口定义文件。）

6. Dart 侧固定文件规范
（此部分与之前版本一致，保持不变。native_bindings.dart, native_types.dart, native_service.dart 位置和内容规则不变。）

7. 包名修改（Android / iOS / macOS）
项目创建时默认包名为 com.example.flutter_native_base，需统一修改为 com.naipingzai.flutter_native_base。

7.1 Android
修改 android/app/build.gradle 中的 applicationId：

gradle
defaultConfig {
    applicationId "com.naipingzai.flutter_native_base"
    ...
}
同时修改 android/app/src/main/AndroidManifest.xml 中的包名（如有必要）以及 MainActivity.kt 的包声明。

7.2 iOS / macOS
修改 ios/Runner/Info.plist 和 macos/Runner/Info.plist 中的 CFBundleIdentifier 为 com.naipingzai.flutterNativeBase（注意 macOS 通常使用反向域名，可保持一致）。

同时修改 Xcode 项目中的 Bundle Identifier（通过 Xcode 或直接修改 .pbxproj，建议用 Xcode 打开后设置）。

7.3 清理与重建
修改后执行：

bash
flutter clean
flutter pub get
flutter build apk  # 验证
8. 编译与自动发现机制
（与之前版本一致，使用 package_ffi 模板自动编译原生代码。）

9. 添加新 FFI 接口的完整示例
（与之前版本一致，步骤不变。）

10. 约束与最佳实践
（与之前版本一致，同时增加国际化与图标约束。）

新增约束：

国际化：所有 UI 文本必须通过 AppLocalizations 获取，禁止硬编码字符串。

图标：必须通过 flutter_launcher_icons 生成各平台图标，源图放在 assets/icons/。

11. 初始化命令（一次性设置）
bash
# 创建项目
flutter create --template=package_ffi --project-name=flutter_native_base \
    --org=com.naipingzai flutter_native_base
cd flutter_native_base

# 创建目录结构
mkdir -p lib/l10n native/include native/src/core native/src/platform assets/icons

# 移动原生源码模板（按需）
# 删除默认示例，替换为自己的 bridge_api.h / bridge_api.c

# 配置 l10n.yaml 和 pubspec.yaml 依赖（见第3、4节）

# 放置图标源文件到 assets/icons/flutter_native_base_logo.jpg

# 生成本地化
flutter gen-l10n

# 生成图标
flutter pub get
flutter pub run flutter_launcher_icons

# 修改包名（见第7节）
# 运行验证
flutter run -d android
12. 验证命令
bash
flutter test
flutter run -d android
flutter run -d ios
flutter run -d windows
flutter run -d macos
flutter run -d linux
检查各平台应用名称、图标、语言切换是否正常。

13. 常见问题
问题	解决
AppLocalizations 未找到	确保执行 flutter gen-l10n，并在 MaterialApp 中正确配置 localizationsDelegates。
图标生成失败	检查 flutter_launcher_icons 版本，确保 image_path 路径正确。
包名不一致导致 Google Play 或 App Store 问题	统一修改所有平台配置（见第7节），并清理重建。
新增接口后 Dart 找不到符号	确认函数名大小写一致，且已在 bindAll() 中 lookup。
iOS 编译找不到符号	在 C 函数前加 __attribute__((used)) 防止被优化。
本技能文档是 flutter_native_base 项目的唯一参考标准，所有开发人员必须严格遵守。
项目仓库：https://github.com/naipingzai/FlutterNativeBase
包名：com.naipingzai.flutter_native_base
更新日期：2026-09-07